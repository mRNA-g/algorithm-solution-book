对数据实现累加操作
accumulate（begin，end，初始值，操作函数）
例（字符串累加）：
std::vector<std::string> words = {"Hello", "World", "C++"};
std::string result = std::accumulate(words.begin(), words.end(), std::string(""),
[](const std::string& a, const std::string& b) {
return a + " " + b;
});