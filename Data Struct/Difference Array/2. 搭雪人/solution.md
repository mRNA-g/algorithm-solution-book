这一题融合了二分查找+差分+前缀和      终主播耗时2.5小时将其手撕

1\.首先，打眼一看，时间轴上的人数，典型的差分＋前缀和

2\.其次，再看具体要求，题目要求输出满足人数的最早时间点，没有满足则返回-1。也就是说我们需要判断有无满足人数的时间点，有就储存上最早的，这个存储最早时间点及判断有无问题可通过一个逆序for循环一个关于相应时间点位置的人数的离散化数组来解决

其中主播踩过的坑：

1\.没注意好数组的上界

2\.没整好map容器的基本用法

3\.错把迭代器当作对象用

4\.对有符号变量与无符号变量之间比较有风险没有概念（终于理解为啥灵神每次遍历数组的时候的多此一举是为啥了（int len = arr.size()））

