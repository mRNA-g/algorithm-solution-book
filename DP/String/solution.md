这题求最大长度，主播第一时间想到的是用双指针去遍历，但想了想，这可能会超时的，于是，主播就想到了动态规划。动态规划三步走。第一步确定dp内容指代什么，由于本题要求返回一段字符串，最后应该要调用substr函数去裁剪目标字符串，该函数需要pos和len，所以主要的更新内容为最长字符串的起始位置pos和长度len

所以dp\[i]\[j]中的i为目标字符串的起始位，j为末尾位，dp内容为字符串i到j是否为回文字符串，如果是就更新当前长度j-i+1是否是最大，如果是则顺带更新pos；注这里面有两种特殊情况 就是当回文字符串长度为1或2的时候

